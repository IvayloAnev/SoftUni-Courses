function solve(input){
    let chart = input ;
    let isUperCase = chart.toUpperCase();
    if(chart === isUperCase ){
    console.log(`upper-case`)
    }else{
    console.log(`lower-case`)
    }
    }

    solve('L')